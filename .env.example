# =============================================================================
# Conway's Game of Life - Environment Configuration
# =============================================================================
# Copy this file to .env and customize values as needed.
#
# NOTE: This application is a client-side only SPA. These variables are 
# embedded at build time, not runtime. Rebuild after changing values.
# =============================================================================

# Base URL for the application (used in production builds)
# Example: /conways/ if hosted at https://example.com/conways/
VITE_BASE_URL=/
